## Project Python dependencies (use a virtual environment)

# Core runtime
pyspark
pandas==2.1.3
numpy==1.24.3
pyarrow==14.0.1

# Web / UI
gradio==4.44.0

# AI (optional)
google-generativeai==0.3.2
python-dotenv==1.0.0

# Cohere SDK (optional) - used for AI-driven explanations when COHERE_API_KEY is set
cohere>=4.0.0

# Metrics
prometheus-client==0.19.0

# Utilities / test data
Faker==20.1.0

# Dev/test
pytest>=7.0

# Notes:
# - Installing pyspark may require Java on your machine (JDK 11+ recommended).
# - google-generativeai is optional and only needed if you use the Gemini integration.
